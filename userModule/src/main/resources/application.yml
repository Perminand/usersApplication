spring:
  security:
    user:
      name: admin
      password: admin
  datasource:
    url: jdbc:postgresql://62.109.21.174/users
    username: user
    password: user
  output:
    ansi:
      enabled: ALWAYS
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  sql:
    init:
      mode: always
